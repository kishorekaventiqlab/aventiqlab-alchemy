{
  "name": "aventiqlab-alchemy",
  "version": "0.1.0",
  "private": true,
  "description": "AventiqLab alchemy — Phase 0 content blueprint + the ASTRA Content Studio generation pipeline service (Phase 2, see docs/adr/0001).",
  "workspaces": [
    "service",
    "video-studio"
  ],
  "scripts": {
    "test": "npm test --workspace service",
    "typecheck": "npm run typecheck --workspace service",
    "dev": "npm run dev --workspace service"
  }
}
