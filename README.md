# aventiqlab-alchemy

This repository's original plan (the ASTRA Content Studio pipeline — `/v1/generate` and `/v1/render`, the video/v2 generation and rendering system) has been retired. The AWS infrastructure that backed it (account `880636108741`, region `ap-south-1`) has been fully torn down, and `main` has been cleared to make room for a new plan.

The complete prior history — source code, docs, and commit history — is preserved on the [`archive/pre-cleanup`](../../tree/archive/pre-cleanup) branch.
